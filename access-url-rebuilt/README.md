# Sync2Access Extension Source

This directory contains the main Chrome Manifest V3 extension source for Sync2Access.

## Commands

```bash
npm ci
npm run dev
npm run build
```

`npm run build` emits the loadable extension into `dist`.

For the full project overview, architecture, security notes, and installation guide, see the repository root [README.md](../README.md).
